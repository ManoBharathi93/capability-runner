# Execution Policies

This directory will contain reviewed, nonsecret allowlist and risk-policy configuration when Policy Guard is implemented in P1.3. Policy data belongs here; policy enforcement remains in `src/capability_runner/interaction/`.