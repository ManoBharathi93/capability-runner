# Application Profiles

This directory will contain reviewed, nonsecret target application profiles when the browser milestone defines their contract. Do not store credentials, cookies, browser state, tenant PII, or private deployment endpoints here.