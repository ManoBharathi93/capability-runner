# Submission Evidence

These files are a curated, sanitized subset of genuine Capability Runner executions. They were
copied byte-for-byte from their runtime directories; generated JSON was not reconstructed or
edited for presentation. Synthetic member data is used throughout.

| Path | Provenance | What it demonstrates |
| --- | --- | --- |
| [through-line/summary.json](through-line/summary.json) | Provider-backed P5.4a.1 reviewer run | Discovery `SUCCESS` in 5 model calls, generated artifact storage, fresh Replay `SUCCESS`, `438221 / USD`, and zero Builder/Replay model calls. |
| [through-line/capabilities/lookup_savings_balance/1.0.0.json](through-line/capabilities/lookup_savings_balance/1.0.0.json) | Generated by Capability Builder from that verified Discovery trace | Versioned contract with a sensitive typed input, four semantic actions, bounded retry, success conditions, typed outputs, and `MEMBER_NOT_FOUND`. |
| [through-line/evidence.jsonl](through-line/evidence.jsonl) | Same P5.4a.1 reviewer run | Discovery lifecycle and bounded semantic decisions, authorized actions, capability build/validate/store/load, and complete fresh Replay lifecycle correlated by run/session/step/action IDs. |
| [exception/summary.json](exception/summary.json) | Fresh model-free P5.4a reviewer run | `BUSINESS_OUTCOME / MEMBER_NOT_FOUND`, two browser actions, and zero model calls. |
| [exception/evidence.jsonl](exception/evidence.jsonl) | Same P5.4a exception run | Complete Replay start, per-step dispatch/completion, and terminal business-outcome lifecycle alongside gateway events. |
| [intervention/summary.json](intervention/summary.json) | Fresh model-free P5.4a reviewer run | Approval, same-session operator action, generation `0 -> 3`, resumed `SUCCESS`, and no repeated automation side effects. |
| [intervention/evidence.jsonl](intervention/evidence.jsonl) | Same P5.4a intervention run | Sanitized capability/step/current-state context plus approval, handoff, operator action, validation, and Replay resume chronology. |
| [failure/evidence.jsonl](failure/evidence.jsonl) | P5.2 `session_expired` Replay composition | Hard `TARGET_NOT_FOUND` plus bounded sanitized visible-state text showing the expired session. |

The through-line log combines the required Discovery and fresh-Replay evidence. Its `session_id`
changes between those phases, while the shared `run_id` preserves correlation. The summary's
relative artifact path is preserved by retaining the original runtime directory structure.

## Privacy and interpretation

The [checking UI summary](checking/ui-summary.json), [generated capability](checking/capability.json),
[Discovery log](checking/discovery.jsonl), and [fresh Replay log](checking/replay.jsonl) are from
the 2026-09-13 real-provider LegacyBank B checking run. Discovery used four model calls and three
actions; Replay returned `15840 / USD` for a different synthetic customer with zero model calls.
These four files were copied unchanged from the run and UI-check output. The capability is a
reviewable definition; its separate generated application binding remains in the local run package.
This proves checking as well as savings, not account creation or three unrelated banking workflows.

The generated capability contains parameter references, not invocation member IDs or balances,
and contains no browser selectors, provider identity, endpoint, transcript, or executable code.
The JSONL omits fill values and provider payloads. The displayed balance is synthetic output and
is retained because it proves typed extraction.

The refreshed provider-backed through-line proves the direct Discovery lifecycle and structured
decision metadata without retaining model rationale, raw prompts/responses, or provider payloads.
Together with the fresh model-free files, it proves complete Replay lifecycle narration and the
sanitized intervention context. The failure signal remains bounded visible-state text, not a
screenshot or DOM archive.
