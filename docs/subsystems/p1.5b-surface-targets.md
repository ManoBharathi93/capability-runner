# Subsystem: Surface abstraction and browser-target contracts

## Status

- Design: **ACCEPTED BASELINE**
- Implementation: `implemented`
- Verification: `passed`
- Authorized milestone: `P1.5b`

## After implementation

### 8. Actual files and behavior

- `src/capability_runner/contracts/surfaces.py` defines `SemanticTargetRef`, `FrameContext`, locator candidates, row-scoped repeated-control targeting, `ApplicationProfile`, and pure locator fallback semantics.
- `src/capability_runner/surfaces/surface_adapter.py` defines the browser surface port as a pure protocol with opaque surface-session references and typed result/evidence seams.
- `tests/unit/contracts/test_surface_contracts.py` proves semantic target validation, binding serialization, row-scoped repeated-control representation, duplicate profile binding rejection, and locator fallback semantics.
- `tests/unit/surfaces/test_surface_adapter_contract.py` proves the adapter surface stays Playwright-free and does not expose raw implementation handles.

### 9. Commands and results

- 2026-09-12, CPython 3.12.10, pytest 9.1.1: `uv run pytest tests/unit/contracts/test_surface_contracts.py tests/unit/surfaces/test_surface_adapter_contract.py` -> passed after the final contract and adapter files were added.

### 10. Known limits and next integration

- Policy Guard still uses `TargetSpec`; the durable semantic target migration is deferred to the later Action Gateway step.
- No actual browser execution, Playwright target resolution, or surface observation runtime is implemented yet.
